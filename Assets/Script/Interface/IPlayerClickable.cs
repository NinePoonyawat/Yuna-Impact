using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public interface IPlayerClickable
{
    // Start is called before the first frame update
    void click(PlayerController playerController);
}
